query($where: crew_permission_bool_exp = {}, $orderBy: [crew_permission_order_by!]) {
  crew_permission(where: $where, order_by: $orderBy) {
    id
    created_at
    updated_at
    crew_id
    crew {
      id
      name
      driver_user_id
    }
    request_id
    status
  }
}
