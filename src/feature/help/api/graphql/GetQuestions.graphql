query GetQuestions($where: user_question_bool_exp, $orderBy: [user_question_order_by!]) {
  user_question(where: $where, order_by: $orderBy) {
    answer_type
    created_at
    id
    message
    status
    user_id
    updated_at
    subject
  }
}