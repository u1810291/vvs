query GetClients($query: String) {
  usersByQuery(query: $query) {
    users {
      active
      birthDate
      email
      firstName
      id
      lastName
      middleName
      mobilePhone
      username
      verified
      fullName
    }
  }
}
