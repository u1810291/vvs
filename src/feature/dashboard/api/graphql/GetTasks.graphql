query GetTasks($where: events_bool_exp) {
  events(where: $where) {
    created_at
    crew_id
    description
    id
    name
    object_id
    provider_id
    provider_name
    status
    reason
    updated_at
    crew {
      name
    }
    object {
      address
      name
    }
  }
}
