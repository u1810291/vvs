query GetCrews {
  crew(order_by: { status: desc }) {
    name
    status
    abbreviation
    id
    user_settings {
      is_online
      last_ping
    }
    permissions {
      comment
      status
      task_can_be_assigned
      updated_at
      id
      created_at
    }
  }
}
