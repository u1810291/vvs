query Objects($where: object_bool_exp, $orderBy: [object_order_by!]) {
  object(where: $where, order_by: $orderBy) {
    address
    city
    contract_no
    contract_object_no
    description
    id
    is_atm
    is_crew_autoasigned
    feedback_sla_time_in_min
    latitude
    longitude
    name
    navision_id
    phone
    provider_id
    created_at
    users {
      user_id
    }
  }
}
