query ObjectById ($id: uuid!) {
  object_by_pk(id: $id) {
    address
    city
    contract_no
    contract_object_no
    description
    id
    is_atm
    latitude
    longitude
    name
    navision_id
    phone
    provider_id
    users {
      user_type
    }
  }
}
