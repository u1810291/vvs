query CrewBreachById ($id: uuid!) {
    crew_breach_by_pk(id: $id) {
        id
        end_time
        start_time
        nodes
        dislocation_nodes
        driver_user_id
        crew {
            name
            id
            status
            # zone {
            #     nodes
            # }
        }
    }
}
