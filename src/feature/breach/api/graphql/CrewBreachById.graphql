query CrewBreachById ($id: uuid!) {
    crew_breach_by_pk(id: $id) {
        id
        end_time
        start_time
        nodes
        crew {
            name
            status
            driver_name
            zone {
                nodes
            }
        }
    }
}
