query GetBreaches($where: crew_breach_bool_exp = {}) {
    crew_breach(where: $where) {
        id
        end_time
        start_time
        nodes
        crew_id
        driver_user_id
        crew {
            name
            status
            zone {
                nodes
            }
        }
    }
}
