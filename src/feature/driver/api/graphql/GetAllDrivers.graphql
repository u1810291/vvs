query GetDrivers {
  usersByRole(role: "crew") {
    users {
      active
      birthDate
      email
      firstName
      id
      lastName
      middleName
      mobilePhone
      username
      verified
      fullName
    }
  }
}
