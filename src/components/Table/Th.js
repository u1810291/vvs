const Th = props => (
  <th className='sticky top-0 z-10 border-b border-gray-300 bg-gray-50 bg-opacity-75 px-3 py-4 text-left text-sm font-semibold text-gray-900 backdrop-blur backdrop-filter' {...props} />
);

export default Th;
