const Th = props => (
  <th className='sticky top-0 z-9 border-t border-gray-200 bg-gray-50 bg-opacity-75 px-3 py-4 text-left text-sm font-normal text-gray-400 backdrop-blur backdrop-filter' {...props} />
);

export default Th;
